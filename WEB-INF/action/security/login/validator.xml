<?xml version='1.0' encoding='ISO-8859-1'?>
<validator onerror="/action/error/validation/ajax">

	<parameter id="userlogin" type="varchar" required="true" label="${lbl:s_profile_login}" maxlen="25" />
	<parameter id="passwd" type="varchar" required="true" label="${lbl:s_fld_password}" maxlen="25" />
	<parameter id="uri" type="varchar" required="true" label="URL to redirect after login" />

	<!-- SIMONE - Evitar que el usuario haga login dos veces -->
	
	<!--
	<custom-validator
		classname='dinamica.validators.DuplicatedKeyValidator'
		on-error-label='${lbl:b_ip_address_doesnt_autorized}.' 
		sql='invalid-ip.sql' 
		id="form_error"/>

	<custom-validator
		classname='dinamica.validators.DuplicatedKeyValidator'
		on-error-label='${lbl:b_session_already_actived}.' 
		sql='invalid-session.sql' 
		id="form_error"/>
	-->

</validator>

